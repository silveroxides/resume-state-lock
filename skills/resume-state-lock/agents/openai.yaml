interface:
  display_name: "Resume State Lock"
  short_description: "Reinitialize safely after context compaction"
  default_prompt: "Use $resume-state-lock to reload applicable rules and pause."
policy:
  allow_implicit_invocation: true
